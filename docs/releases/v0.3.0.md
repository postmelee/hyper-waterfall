# v0.3.0 release 문서

이 문서는 Hyper-Waterfall `v0.3.0` release 정합성, npm package 구성, publish 승인 게이트, publish 실행 결과, publish 후 검증 기준을 기록한다. GitHub Release/tag와 npm publish는 Task #81에서 완료했으며, Homebrew tap 갱신은 별도 후속 작업으로 분리한다.

## 목적

`v0.3.0`은 M050 다국어 적용 지원 release다. 기본 README는 English로 제공하고, Korean과 Simplified Chinese README 및 lifecycle 진입 문서를 함께 배포한다. 신규 적용과 기존 update 판단은 선택 locale, fallback 후보, locale manifest diff, 기존 locale 보존 또는 전환 요청을 파일 변경 전에 보고한다.

## canonical 기준

| 항목 | 값 | 확인 관점 |
|---|---|---|
| framework version | `0.3.0` | `package.json`, `templates/manifest.json`, version 기록 예시가 같은 값을 가리켜야 한다. |
| release tag | `v0.3.0` | `templates/manifest.json`의 `release.plannedTag`와 release notes 대상 tag가 같아야 한다. |
| baseline tag | `v0.2.0` | migration guide의 시작 version과 `templates/manifest.json`의 `release.baselineTag`가 같아야 한다. |
| release status | `released` | GitHub Release/tag와 npm publish 완료 상태와 `templates/manifest.json`의 `release.status`가 같아야 한다. |
| canonical source | `github-release` | 적용 저장소 version 기록 예시의 `source`와 manifest release source가 같아야 한다. |
| migration guide | `docs/migrations/v0.2.0-to-v0.3.0.md` | locale 판단, manual/Skill semantic review, update PR 본문 반영 기준을 포함해야 한다. |
| supported locales | `en`, `ko`, `zh-CN` | README, entrypoint, lifecycle docs, `templates/locales` source가 같은 locale 집합을 가리켜야 한다. |

## release 상태 (2026-05-28)

| 영역 | 상태 | 근거 |
|---|---|---|
| package metadata | 준비 완료 | `package.json` version은 `0.3.0`이고 npm tarball `files`에 `README.ko.md`, `README.zh-CN.md`를 포함한다. |
| manifest metadata | 공개 완료 | `templates/manifest.json`은 `frameworkVersion: 0.3.0`, `plannedTag: v0.3.0`, `baselineTag: v0.2.0`, `status: released`다. |
| README 다국어 진입 | 준비 완료 | `README.md`, `README.ko.md`, `README.zh-CN.md`가 language support, locale prompt, `npx hyper-waterfall@0.3.0` dry-run을 제공한다. |
| localized lifecycle docs | 준비 완료 | `docs/agent-entrypoint.en.md`, `docs/agent-entrypoint.zh-CN.md`, `docs/lifecycle/*.en.md`, `docs/lifecycle/*.zh-CN.md`가 추가됐다. |
| migration guide | 준비 완료 | `docs/migrations/v0.2.0-to-v0.3.0.md`가 기존 `v0.2.0` 적용 저장소의 수동 확인과 locale 보존/전환 기준을 설명한다. |
| npm publish | 공개 완료 | `hyper-waterfall@0.3.0`이 npm registry에 publish됐고 `latest` dist-tag가 `0.3.0`을 가리킨다. |
| GitHub Release/tag | 공개 완료 | `v0.3.0` tag와 GitHub Release가 `83836828a4da24385d0410515d35ee43946b981f` 기준으로 생성됐다. |
| Homebrew tap | 후속 필요 | 현재 public tap formula는 `0.2.0` tarball을 가리키므로 npm publish 후 formula URL, version, checksum 갱신이 필요하다. |

## publish 전 승인 게이트 (완료)

다음 항목은 `npm publish`, `git tag v0.3.0`, `gh release create` 전에 확인한 승인 게이트다. Homebrew tap 갱신은 이번 release 실행에서 제외했다.

- [x] Task #79 PR이 `main`에 merge됐다.
- [x] `package.json` version, `templates/manifest.json` version, release tag, migration guide target이 `0.3.0` / `v0.3.0`으로 일치한다.
- [x] `node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-en --manifest "$(pwd)/templates/manifest.json" --locale en --dry-run`이 통과한다.
- [x] `node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-ko --manifest "$(pwd)/templates/manifest.json" --locale ko --dry-run`이 통과한다.
- [x] `node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-zh --manifest "$(pwd)/templates/manifest.json" --locale zh-CN --dry-run`이 통과한다.
- [x] `npm test`가 통과한다.
- [x] `npm_config_cache=/private/tmp/hyper-waterfall-npm-cache npm pack --dry-run`이 통과한다.
- [x] `npm pack --dry-run` 결과에 다국어 README, localized docs, `templates/locales`가 포함된다.
- [x] 작업지시자가 GitHub Release/tag 생성과 npm publish 실행을 별도 승인했다.

## npm tarball 구성 체크리스트

`npm pack --dry-run` 출력에서 최소한 다음 항목을 확인한다.

- [x] `bin/hyper-waterfall.js`
- [x] `src/cli.js`
- [x] `src/commands/init.js`
- [x] `src/commands/update.js`
- [x] `src/commands/doctor.js`
- [x] `templates/manifest.json`
- [x] `templates/locales/en/AGENTS.md`
- [x] `templates/locales/ko/AGENTS.md`
- [x] `templates/locales/zh-CN/AGENTS.md`
- [x] `templates/locales/en/mydocs/manual/`
- [x] `templates/locales/ko/mydocs/manual/`
- [x] `templates/locales/zh-CN/mydocs/manual/`
- [x] `docs/agent-entrypoint.md`
- [x] `docs/agent-entrypoint.en.md`
- [x] `docs/agent-entrypoint.zh-CN.md`
- [x] `docs/lifecycle/adoption.en.md`
- [x] `docs/lifecycle/adoption.zh-CN.md`
- [x] `docs/lifecycle/update.en.md`
- [x] `docs/lifecycle/update.zh-CN.md`
- [x] `docs/lifecycle/update_pr.en.md`
- [x] `docs/lifecycle/update_pr.zh-CN.md`
- [x] `docs/localization.md`
- [x] `docs/localization-smoke.md`
- [x] `docs/migrations/v0.2.0-to-v0.3.0.md`
- [x] `docs/releases/v0.3.0.md`
- [x] `README.md`
- [x] `README.ko.md`
- [x] `README.zh-CN.md`
- [x] `LICENSE`
- [x] `package.json`

Stage 2 package dry-run 결과:

| 항목 | 결과 |
|---|---|
| package name | `hyper-waterfall` |
| package version | `0.3.0` |
| tarball filename | `hyper-waterfall-0.3.0.tgz` |
| package size | `1.8 MB` |
| unpacked size | `2.5 MB` |
| total files | `218` |
| 다국어 README 포함 | `README.md`, `README.ko.md`, `README.zh-CN.md` |
| localized docs 포함 | `docs/agent-entrypoint.en.md`, `docs/agent-entrypoint.zh-CN.md`, `docs/lifecycle/*.en.md`, `docs/lifecycle/*.zh-CN.md` |
| locale pack 포함 | `templates/locales/en`, `templates/locales/ko`, `templates/locales/zh-CN` |

## release 실행 결과

| 항목 | 결과 |
|---|---|
| release commit | `83836828a4da24385d0410515d35ee43946b981f` |
| Git tag | `v0.3.0` |
| GitHub Release | <https://github.com/postmelee/hyper-waterfall/releases/tag/v0.3.0> |
| npm package | `hyper-waterfall@0.3.0` |
| npm tarball | `https://registry.npmjs.org/hyper-waterfall/-/hyper-waterfall-0.3.0.tgz` |
| npm bin | `{ "hyper-waterfall": "bin/hyper-waterfall.js" }` |
| npm dist-tag | `latest -> 0.3.0` |
| npm shasum | `e2fdc132cf8387c806d711f6c9da91d9a81ff012` |

## release notes 기준

제목:

```text
v0.3.0 - 다국어 적용 진입점과 locale pack
```

본문 초안:

```md
## Summary

`v0.3.0` adds multilingual adoption support for Hyper-Waterfall. English is the default README and locale, while Korean and Simplified Chinese are available through localized README files, lifecycle entrypoints, and template locale packs.

## Highlights

- Added language links and locale-specific AI prompts to README.
- Added Korean and Simplified Chinese README files alongside the default English README.
- Added localized lifecycle entrypoints and adoption/update/update PR guides.
- Added locale-aware manifest contracts for `en`, `ko`, and `zh-CN`.
- Preserved `.hyper-waterfall/version.json` as the selected locale record.
- Kept npm CLI, Homebrew, Docker, and plugins as execution channels rather than canonical sources.

## Update Basis

- Migration guide: `docs/migrations/v0.2.0-to-v0.3.0.md`
- Manifest: `templates/manifest.json`
- Applied repository version/locale record: `.hyper-waterfall/version.json`

## Notes

- Existing `v0.2.0` repositories without a locale record report current locale as `unknown`; maintainers approve whether to preserve Korean context as `ko` or switch locale.
- File changes still require the normal Hyper-Waterfall approval workflow.
```

## publish 후 확인

publish 후에는 빈 임시 디렉터리에서 다음을 확인한다.

```bash
npm view hyper-waterfall@0.3.0 version
npm view hyper-waterfall@0.3.0 dist.tarball
npm view hyper-waterfall@0.3.0 bin --json
npx hyper-waterfall@0.3.0 --version
npx hyper-waterfall@0.3.0 --help
npx hyper-waterfall@0.3.0 init --repo /private/tmp/hw-postpublish-en --locale en --dry-run
npx hyper-waterfall@0.3.0 init --repo /private/tmp/hw-postpublish-ko --locale ko --dry-run
npx hyper-waterfall@0.3.0 init --repo /private/tmp/hw-postpublish-zh --locale zh-CN --dry-run
```

기대 결과:

- [x] npm registry version이 `0.3.0`이다.
- [x] `dist.tarball` URL이 `hyper-waterfall-0.3.0.tgz`를 가리킨다.
- [x] `bin.hyper-waterfall`이 `bin/hyper-waterfall.js`를 가리킨다.
- [x] `--version` 출력이 `0.3.0`이다.
- [x] `--help` 출력에 `init`, `update`, `doctor`, canonical 기준 안내가 포함된다.
- [x] `en`, `ko`, `zh-CN` init dry-run이 locale source와 승인 요청을 출력한다.

## 보류 항목

- 승인 없는 Homebrew tap formula 갱신
- Homebrew core 제출
- Docker image 구현과 registry 배포
- Codex official public plugin 배포
- Claude Official directory 제출과 release asset 게시
- 자동 release pipeline
- write mode 자동 적용

## 검증 기준

release 문서 자체의 최소 검증:

```bash
rg -n '0.3.0|v0.3.0|locale|README.ko.md|README.zh-CN.md|npm pack|npm publish|승인|Homebrew|Docker|plugin' docs/releases/v0.3.0.md
git diff --check
```

release 실행 전 실행한 최소 검증:

```bash
npm test
node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-en --manifest "$(pwd)/templates/manifest.json" --locale en --dry-run
node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-ko --manifest "$(pwd)/templates/manifest.json" --locale ko --dry-run
node bin/hyper-waterfall.js init --repo /private/tmp/hw-readiness-zh --manifest "$(pwd)/templates/manifest.json" --locale zh-CN --dry-run
npm_config_cache=/private/tmp/hyper-waterfall-npm-cache npm pack --dry-run
git diff --check
git status --short --branch
```
